# Database scripts. The site never loads these - they are pasted into the
# Supabase SQL editor by hand - so they have no business on a web host.
# supabase-setup.sql also has your email in its owner line.
*.sql

# Setup and operating notes, only useful to you
SETUP.md

# Stale, describes an older version of this project
README.md

# Local harnesses that fill the tools with fake rows. The pattern covers the
# shared stub script too, which is why it is not just _h_*.html any more.
_h_*
_harness.html
